{
  "_comment": "Copy this file to secrets.local.json (same folder) and fill in real values. secrets.local.json is gitignored — NEVER commit real credentials. You can also use CASE_CENTER_API_KEY / CASE_CENTER_COOKIE env vars instead of this file.",
  "apiKey": "PASTE-YOUR-API-KEY-HERE",
  "cookie": "PASTE-YOUR-COOKIE-HERE"
}
