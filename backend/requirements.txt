# Core API + DB stack (works as-is on SQLite for demos).
fastapi>=0.110
uvicorn[standard]>=0.29
SQLAlchemy>=2.0
alembic>=1.13

# Production database drivers.
# Both are pure-Python / prebuilt wheels (no native build), so installing them is harmless for
# the SQLite demo; each is only USED when DATABASE_URL points at its backend. psycopg is also
# required for Render (Postgres).
psycopg[binary]>=3.1     # PostgreSQL  (postgresql+psycopg://...)
PyMySQL>=1.1             # MySQL/MariaDB (mysql+pymysql://...)
